<h1><%= @playlist.title %></h1>

<ul>
    <% @playlist.tracks.each do |track| %>
        <li><% track.name %></li> 
    <% end %>
</ul>

#add ability to play songs

<%= button_to "ADD SONGS", add_songs_path, method: :get %>