<h1><%= @playlist.title %></h1>

<ul>
    <% @playlist.tracks.each do |track| %>
        <li><%= track.name %></li> 
        <%= button_to  "Remove Song From Playlist", {:controller => :tracks, :action => 'destroy', :id => track.id }, :method => :delete %>
    <% end %>
</ul>
